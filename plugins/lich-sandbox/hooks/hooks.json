{
  "description": "lich-sandbox hooks — advisory-only, never blocking. Chained after lich-core PostToolUse fires (bash dispatch.sh reads review-flags.jsonl and triggers this sub-plugin). See @shared/vis/conduct/hooks.md.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/dispatch.sh\" lich-sandbox",
            "timeout": 30000
          }
        ]
      }
    ]
  }
}
